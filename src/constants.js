const usageText = `
Usage
  $ envt edit <environment>

Options
  --port, -p\t\t\tPort where the web app is running, default: 5000

Examples
  $ envt edit production
`

module.exports = {
  usageText,
}
